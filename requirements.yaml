name: pytorch

channels:
  - pytorch

dependencies:
  - python=3.6
  - matplotlib
  - pytorch
  - cudatoolkit
  - pytest
  - pip
  - pip:
    - mido
    - git+https://github.com/craffel/pretty-midi.git
    - tensorboardx
    - python-osc
